# PRODUCTION

services:
  api:
    container_name: smart_water_purifier_api_prod
    restart: always

  mqtt-worker:
    container_name: smart_water_purifier_mqtt_worker_prod
    restart: always

  alert-worker:
    container_name: smart_water_purifier_alert_worker_prod
    restart: always

  db:
    container_name: smart_water_purifier_db_prod
    restart: always

  redis:
    container_name: smart_water_purifier_redis_prod
    restart: always

  mqtt:
    container_name: smart_water_purifier_mqtt_prod
    restart: always
    
  prometheus:
    container_name: smart_water_purifier_prometheus_prod
    restart: always

  grafana:
    container_name: smart_water_purifier_grafana_prod
    restart: always
